## Operation: Use %timeit in IPython for quick timing

# In an IPython/Jupyter environment, use the magic command %timeit
# Example:
# %timeit sum(range(10000))

# Output:
# This will show the average time it takes to run the command multiple times.
